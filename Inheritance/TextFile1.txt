Constructor : It is special method.
              It will not return type.
              Its name className both Same.
              Contructor will be invoked autmatically when the object is created.
              Constructor can be used to create the object.
              It is of different types.
              Instance constructor
                 Instance constructor can be invoked for every object
                 Constructor with paramter is Called Default Constructor
                 Parameterised Constructor
               
               
               static constructor
                 static constructor can be invoked only once
Destructor : 
            It is a special method, it's name and class name both are same with tilte operator(~)
            accessmodifier can't be used.

            It is used destroy the objecct from memory

            It will not have return type.
            It will be called automaically after completing the functionalites of the object.


base : In order to access  parent class methdos and to transfer the data from child class constructor to parent class constructor















