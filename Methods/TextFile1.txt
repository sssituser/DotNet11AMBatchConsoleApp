Method (Function) : It is a self contained block of stmts or instructions, which can be 
used to pefrorm Paraticular task is called a method.

Methods are of two types


1. Predefined Methods :
		The methods which present in the .dll files or predefined namespaces  or public Assmeblies
		are called Predefined Methods.

		Example : Console.WriteLine()
				  Console.ReadLine() etc......

		String is a class, In order to work with the strings we use predefined methods of
		a string class.

		String is immutable.
		Class can hold both static methods and non static methods
		static methods can be called using className
		non static methods can be called using object.


2. User defined Methods : The mehods which are defined by the developers in the Applications re
called user defined metods.
	
	1.Define
		AccessModifer RetrunType MethodName(Parameters){
			return val;
		}


		1.AccessModifer : public static
		2.ReturnTypes   : any
		3.MethodName	: MeaningFul, It Should give the recemble the task
		4.ParaMeters	: void,string,int ...any

	2.call(Consuming the Method)


	Write a program to add to numbers.

	AM : public static
	MN : Add,Sum
	RT : int-1
	PM : 2 integers

	public static int Sum(int num1,int num2){
		num1+num2
	}

	AM : public static
	MN : Sub,Diff
	RT : int
	PM : 2-integers

	public static int Diff(int num1,int num2){
		num1-num2;
	}

	






